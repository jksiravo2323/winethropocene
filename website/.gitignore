# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*

# Editors
.vscode/
.idea/
*.swp
*.swo

# Node / build artefacts (for Phase 2 Astro)
node_modules/
dist/
.astro/
.cache/
.parcel-cache/
.turbo/

# Python venv (atlas-renderer pipeline)
.venv/

# 3D source models + mesh exports. Source blends and the redundant .gltf stay
# local; the assets the live atlas globe + 2D square map load are tracked below.
source_models/
public/meshes/*
!public/meshes/globe.glb
!public/meshes/earth-land.png
!public/meshes/earth-elevation.png
!public/meshes/earth-water.png

# Local environment + secrets
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Cloudflare / hosting local state
.wrangler/
.vercel/
.netlify/

.dev.vars*
!.dev.vars.example
!.env.example

# Local npm cache (cache=.npm-cache in .npmrc)
.npm-cache/
